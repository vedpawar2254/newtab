---
phase: 02-core-editor
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - tests/editor/helpers.ts
  - tests/editor/formatting.test.ts
  - tests/editor/input-rules.test.ts
  - tests/editor/code-block.test.ts
  - tests/editor/table.test.ts
  - tests/editor/slash-command.test.ts
  - tests/editor/autosave.test.ts
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-10, EDIT-11, EDIT-12]

must_haves:
  truths:
    - "Editor test helper creates a Tiptap instance with all extensions for testing"
    - "Formatting tests verify bold, italic, underline, strikethrough, heading, and list toggles"
    - "Markdown input rule tests verify # -> H1, ** -> bold, - -> bullet, 1. -> ordered, [] -> task"
    - "Code block tests verify language attribute changes"
    - "Table tests verify row/column insertion commands"
    - "Slash command tests verify item filtering and command execution"
    - "Auto-save tests verify content serialization with schema versioning"
    - "All tests pass with npx vitest run"
  artifacts:
    - path: "tests/editor/helpers.ts"
      provides: "Shared test utilities for creating editor instances with all extensions"
      exports: ["createTestEditor"]
    - path: "tests/editor/formatting.test.ts"
      provides: "Tests for EDIT-01 (rich text) and EDIT-12 (keyboard shortcuts)"
    - path: "tests/editor/input-rules.test.ts"
      provides: "Tests for EDIT-02 (markdown shortcuts) and EDIT-03 (headings)"
    - path: "tests/editor/autosave.test.ts"
      provides: "Tests for schema versioning and content serialization"
  key_links:
    - from: "tests/editor/helpers.ts"
      to: "lib/editor/extensions.ts"
      via: "import createExtensions for test editor setup"
      pattern: "createExtensions"
---

<objective>
Build the editor test suite covering all EDIT-* requirements and verify the complete editor visually with a human checkpoint.

Purpose: Tests validate that formatting, markdown shortcuts, code blocks, tables, slash commands, and auto-save all work correctly. The human checkpoint catches visual/interaction issues that automated tests cannot.

Output: Passing test suite covering editor functionality, visual confirmation of the complete editor experience.
</objective>

<execution_context>
@/Users/vedpawar2254/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedpawar2254/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-editor/02-CONTEXT.md
@.planning/phases/02-core-editor/02-VALIDATION.md
@.planning/phases/02-core-editor/02-01-SUMMARY.md
@.planning/phases/02-core-editor/02-02-SUMMARY.md
@.planning/phases/02-core-editor/02-03-SUMMARY.md
@.planning/phases/02-core-editor/02-04-SUMMARY.md

<interfaces>
From lib/editor/extensions.ts:
```typescript
export function createExtensions(): Extension[];
```

From lib/editor/schema-migrations.ts:
```typescript
export const CURRENT_SCHEMA_VERSION: number;
export function serializeContent(editor: Editor): string;
export function deserializeContent(raw: string): any;
```

From lib/editor/slash-items.ts:
```typescript
export interface SlashItem { title: string; description?: string; icon: string; category: string; command: (editor: Editor) => void; }
export const slashItems: SlashItem[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create editor test helpers and write comprehensive test suite</name>
  <files>tests/editor/helpers.ts, tests/editor/formatting.test.ts, tests/editor/input-rules.test.ts, tests/editor/code-block.test.ts, tests/editor/table.test.ts, tests/editor/slash-command.test.ts, tests/editor/autosave.test.ts</files>
  <read_first>
    - vitest.config.ts (test configuration, environment)
    - lib/editor/extensions.ts (extension array to use in tests)
    - lib/editor/schema-migrations.ts (serialization functions)
    - lib/editor/slash-items.ts (slash item definitions)
    - tests/storage-service.test.ts (existing test patterns for reference)
    - .planning/phases/02-core-editor/02-VALIDATION.md (test map)
  </read_first>
  <action>
Create `tests/editor/helpers.ts`:
- Export `createTestEditor(content?: any)` function that:
  - Creates an Editor instance with `createExtensions()` from `lib/editor/extensions`
  - Sets jsdom environment (Vitest config should have `environment: 'jsdom'`)
  - Returns the editor instance
  - Uses `Editor` from `@tiptap/core` (not `useEditor` hook -- tests run outside React)
- Export `typeText(editor: Editor, text: string)` helper that inserts text at cursor
- Export `simulateInputRule(editor: Editor, text: string)` helper that types text character by character to trigger input rules (input rules fire on text input, not on setContent)

Create `tests/editor/formatting.test.ts` (covers EDIT-01, EDIT-12):
```typescript
describe('Rich text formatting', () => {
  test('toggleBold adds bold mark to selected text', () => {
    // Create editor with "hello world"
    // Select "world"
    // Run editor.chain().focus().toggleBold().run()
    // Assert editor.isActive('bold') is true
    // Assert editor.getJSON() contains mark type 'bold'
  });

  test('toggleItalic adds italic mark', () => { /* similar */ });
  test('toggleUnderline adds underline mark', () => { /* similar */ });
  test('toggleStrike adds strikethrough mark', () => { /* similar */ });

  test('toggleHeading sets heading level', () => {
    // editor.chain().focus().toggleHeading({ level: 1 }).run()
    // Assert editor.isActive('heading', { level: 1 })
  });

  test('toggleBulletList creates bullet list', () => {
    // editor.chain().focus().toggleBulletList().run()
    // Assert editor.isActive('bulletList')
  });

  test('toggleOrderedList creates ordered list', () => { /* similar */ });
  test('toggleTaskList creates task list', () => { /* similar */ });

  test('toggleCode adds inline code mark', () => { /* similar */ });
});
```

Create `tests/editor/input-rules.test.ts` (covers EDIT-02, EDIT-03, EDIT-04, EDIT-05):
- Note: Tiptap input rules fire when text is inserted that matches a pattern. In tests, use `editor.commands.insertContent()` or simulate typing.
```typescript
describe('Markdown input rules', () => {
  test('# followed by space converts to H1', () => {
    // Insert '# ' at start of empty paragraph
    // Assert the node type is 'heading' with level 1
  });
  test('## converts to H2', () => { /* similar */ });
  test('### converts to H3', () => { /* similar */ });
  test('**text** converts to bold', () => {
    // This is a markInputRule -- insert **text** and assert bold mark
  });
  test('- followed by space creates bullet list', () => { /* similar */ });
  test('1. followed by space creates ordered list', () => { /* similar */ });
  test('[] followed by space creates task list', () => {
    // TaskItem input rule
  });
});
```

Create `tests/editor/code-block.test.ts` (covers EDIT-06):
```typescript
describe('Code blocks', () => {
  test('toggleCodeBlock creates a code block', () => {
    // editor.chain().focus().toggleCodeBlock().run()
    // Assert editor.isActive('codeBlock')
  });
  test('code block accepts language attribute', () => {
    // Insert code block, set language to 'javascript'
    // Assert node attrs contain language: 'javascript'
  });
});
```

Create `tests/editor/table.test.ts` (covers EDIT-07):
```typescript
describe('Tables', () => {
  test('insertTable creates a 3x3 table with header', () => {
    // editor.chain().focus().insertTable({ rows: 3, cols: 3, withHeaderRow: true }).run()
    // Assert editor.getJSON() contains type 'table'
    // Assert it has 3 rows
  });
  test('addRowAfter adds a row', () => {
    // Insert table, place cursor in a cell, addRowAfter
    // Assert row count increased by 1
  });
  test('addColumnAfter adds a column', () => {
    // Insert table, place cursor in a cell, addColumnAfter
    // Assert column count increased by 1
  });
});
```

Create `tests/editor/slash-command.test.ts` (covers EDIT-11):
```typescript
describe('Slash command items', () => {
  test('slashItems contains all required categories', () => {
    // Import slashItems
    // Assert categories include: Text, Lists, Media, Advanced
  });
  test('slashItems contains required block types', () => {
    // Assert items include: Paragraph, Heading 1, Bullet List, Code Block, Table, Image, Blockquote, Horizontal Rule
  });
  test('filtering items by query works', () => {
    // Filter slashItems where title matches 'head'
    // Assert only heading items remain
  });
  test('each item command is a function', () => {
    // Assert all items have command property that is a function
  });
});
```

Create `tests/editor/autosave.test.ts` (covers persistence):
```typescript
describe('Schema versioning', () => {
  test('serializeContent wraps editor JSON with schema version', () => {
    // Create editor with content
    // Call serializeContent(editor)
    // Parse result, assert it has schemaVersion and doc fields
  });
  test('deserializeContent unwraps versioned content', () => {
    // Create a versioned JSON string
    // Call deserializeContent(json)
    // Assert it returns the doc object
  });
  test('deserializeContent handles empty string', () => {
    // deserializeContent('') returns null
  });
  test('deserializeContent handles legacy unversioned content', () => {
    // Pass a plain Tiptap JSON without schemaVersion wrapper
    // Should treat as v1 and return it
  });
});
```

Ensure vitest.config.ts has `environment: 'jsdom'` for these tests (should already be set from Phase 1).
  </action>
  <verify>
    <automated>cd /Users/vedpawar2254/Desktop/personalP/newtab/newtab && npx vitest run tests/editor/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/editor/helpers.ts contains `createTestEditor` export
    - tests/editor/formatting.test.ts contains `toggleBold`, `toggleItalic`, `toggleUnderline`
    - tests/editor/input-rules.test.ts contains `heading` with `level: 1`
    - tests/editor/code-block.test.ts contains `codeBlock` or `toggleCodeBlock`
    - tests/editor/table.test.ts contains `insertTable` and `addRowAfter`
    - tests/editor/slash-command.test.ts contains `slashItems`
    - tests/editor/autosave.test.ts contains `serializeContent` and `deserializeContent`
    - `npx vitest run tests/editor/` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All editor tests pass covering formatting, input rules, code blocks, tables, slash commands, and auto-save serialization</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete editor experience</name>
  <files>none</files>
  <action>
Present the complete editor to the user for visual and interactive verification. The user will test all editor features in the running Chrome extension.

What was built across Plans 01-04:
- Tiptap editor with rich text formatting, markdown shortcuts, headings, lists, task lists
- Floating bubble toolbar on text selection with Bold, Italic, Underline, Strikethrough, Code, Link, Heading buttons
- Slash command menu triggered by / with categorized block types, filtering, and keyboard navigation
- Code blocks with syntax highlighting, language selector, and copy button
- Tables with header row, cell selection, column resize, and + buttons for adding rows/columns
- Images via paste/drop stored as base64
- Bookmark cards with OG metadata fetching from background service worker
- Drag-and-drop block reordering with hover grip handles
- Auto-save to IndexedDB with schema versioning
  </action>
  <verify>Human visual confirmation</verify>
  <done>User confirms all editor features work correctly in the running Chrome extension</done>
  <what-built>Complete Notion-quality block editor with rich text formatting, markdown shortcuts, bubble toolbar, slash command menu, code blocks with syntax highlighting and language selector, tables with add controls, images (paste/drop), bookmark cards, and drag-and-drop block reordering. All auto-saved to IndexedDB.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `chrome://extensions` -> Load unpacked -> select `.output/chrome-mv3-dev`
    2. Open a new tab. The editor should appear in the main content area.
    3. **Title:** Click the title field -- "Untitled" placeholder should be visible. Type a title. Press Enter to move to body.
    4. **Rich text:** Type some text. Select it. Bubble toolbar should appear above selection with Bold, Italic, Underline, Strikethrough, Code, Link, Heading buttons. Click Bold -- text should become bold.
    5. **Markdown shortcuts:** Type `# ` at start of a line -- should convert to H1. Type `- ` -- should create bullet list. Type `**bold**` -- should convert to bold text.
    6. **Slash menu:** Type `/` -- slash menu should appear below cursor. Use arrow keys to navigate. Type "code" to filter. Press Enter on "Code Block" -- should insert a code block.
    7. **Code block:** In the code block, type some code. The language selector should be visible at top-right. Change language to "JavaScript" -- syntax highlighting should update. Hover the code block -- copy button should appear. Click copy -- should change to checkmark.
    8. **Table:** Type `/` and select Table. A 3x3 table should appear with header row. Click cells to edit. Tab between cells. Hover table edge -- + buttons should appear. Click + below to add row, + right to add column.
    9. **Task list:** Type `/` and select Todo List. Checkbox should appear. Click checkbox to toggle. Checked text should strikethrough.
    10. **Images:** Paste an image from clipboard -- should appear inline. (Or use slash menu Image option.)
    11. **Drag blocks:** Hover any block -- drag handle grip should appear on left. Drag a block to reorder.
    12. **Auto-save:** Type content, close tab, reopen new tab -- content should persist.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the editor works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests (editor tests + Phase 1 tests)
- Human confirms visual/interactive quality of all editor features
- Content persists across tab closes
</verification>

<success_criteria>
1. All automated editor tests pass
2. Human verifies rich text formatting works via toolbar and keyboard shortcuts
3. Human verifies markdown shortcuts auto-convert
4. Human verifies slash menu opens, filters, and inserts blocks
5. Human verifies code blocks, tables, images, bookmark cards, and drag handles work
6. Human verifies auto-save persists content
7. Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-05-SUMMARY.md`
</output>
