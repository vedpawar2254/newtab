---
phase: 02-core-editor
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/editor/BubbleToolbar.tsx
  - components/editor/SlashCommandMenu.tsx
  - components/editor/SlashCommandList.tsx
  - lib/editor/extensions/slash-command.ts
  - lib/editor/slash-items.ts
  - components/editor/Editor.tsx
  - lib/editor/extensions.ts
autonomous: true
requirements: [EDIT-11, EDIT-01, EDIT-03]

must_haves:
  truths:
    - "Floating bubble toolbar appears above text selection with formatting buttons"
    - "Toolbar disappears when selection is empty, with 100ms delay to prevent flicker"
    - "Toolbar buttons toggle bold, italic, underline, strikethrough, code, link, and heading level"
    - "Active formatting state is highlighted with accent color on toolbar buttons"
    - "Typing / opens a slash command menu below the cursor"
    - "Slash menu shows categorized block types with icons (Text, Lists, Media, Advanced)"
    - "User can navigate slash menu with arrow keys, select with Enter, dismiss with Escape"
    - "Typing after / filters the menu items in real time"
    - "Selecting a slash menu item inserts the corresponding block type"
  artifacts:
    - path: "components/editor/BubbleToolbar.tsx"
      provides: "Floating formatting toolbar on text selection"
      exports: ["BubbleToolbar"]
    - path: "components/editor/SlashCommandMenu.tsx"
      provides: "Slash command popup rendered via Suggestion plugin"
      exports: ["SlashCommandMenu"]
    - path: "lib/editor/extensions/slash-command.ts"
      provides: "Custom Tiptap extension using @tiptap/suggestion"
      exports: ["SlashCommand"]
    - path: "lib/editor/slash-items.ts"
      provides: "Slash menu item definitions with categories, icons, commands"
      exports: ["slashItems", "SlashItem"]
  key_links:
    - from: "components/editor/BubbleToolbar.tsx"
      to: "editor.isActive / editor.chain().focus().toggle*()"
      via: "Tiptap editor commands"
      pattern: "editor\\.chain\\(\\)\\.focus\\(\\)"
    - from: "lib/editor/extensions/slash-command.ts"
      to: "components/editor/SlashCommandMenu.tsx"
      via: "Suggestion render callback"
      pattern: "ReactRenderer"
    - from: "lib/editor/extensions.ts"
      to: "lib/editor/extensions/slash-command.ts"
      via: "import SlashCommand into extension array"
      pattern: "SlashCommand"
---

<objective>
Build the floating bubble toolbar for text formatting and the slash command menu for block insertion. These are the two primary interactive overlays that make the editor feel Notion-quality.

Purpose: The bubble toolbar delivers inline formatting (EDIT-01, EDIT-03 toolbar portion) and the slash menu delivers block insertion (EDIT-11) -- together they are the primary editing interface.

Output: Floating toolbar on text selection with all formatting buttons, slash command popup with categorized block types, full keyboard navigation.
</objective>

<execution_context>
@/Users/vedpawar2254/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedpawar2254/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-editor/02-CONTEXT.md
@.planning/phases/02-core-editor/02-RESEARCH.md
@.planning/phases/02-core-editor/02-UI-SPEC.md
@.planning/phases/02-core-editor/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: Editor component and extensions -->

From components/editor/Editor.tsx (created in Plan 01):
```typescript
// Editor uses useEditor hook -- BubbleToolbar and SlashCommandMenu
// will be rendered as children within the Editor component
export function Editor(): JSX.Element;
```

From lib/editor/extensions.ts (created in Plan 01):
```typescript
// Extension array that needs SlashCommand added
export function createExtensions(): Extension[];
```

From @tiptap/react (library):
```typescript
import { BubbleMenu } from '@tiptap/react/menus';
// BubbleMenu props: editor, shouldShow, children
```

From @tiptap/suggestion (library):
```typescript
// Suggestion utility for building slash command detection
import Suggestion from '@tiptap/suggestion';
// Requires: char, command, render (with onStart, onUpdate, onKeyDown, onExit)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bubble toolbar with formatting buttons and heading dropdown</name>
  <files>components/editor/BubbleToolbar.tsx, components/editor/Editor.tsx</files>
  <read_first>
    - components/editor/Editor.tsx (current editor component to add BubbleToolbar into)
    - .planning/phases/02-core-editor/02-UI-SPEC.md (bubble toolbar specs: colors, sizing, button order, animations, exclusion zones, accessible labels)
    - .planning/phases/02-core-editor/02-RESEARCH.md (BubbleMenu import from @tiptap/react/menus, shouldShow pattern)
  </read_first>
  <action>
Create `components/editor/BubbleToolbar.tsx`:
- Import `BubbleMenu` from `@tiptap/react/menus`
- Import lucide-react icons: `Bold`, `Italic`, `Underline`, `Strikethrough`, `Code`, `Link`, `Heading1`, `Heading2`, `Heading3`, `ChevronDown`
- Accept `editor` prop (Editor type from @tiptap/react)
- Use `BubbleMenu` component with these props:
  - `editor={editor}`
  - `shouldShow={({ editor, state }) => { const { from, to } = state.selection; if (from === to) return false; if (editor.isActive('codeBlock')) return false; if (editor.isActive('image')) return false; return true; }}`
  - `tippyOptions={{ duration: [150, 100], delay: [0, 100] }}` (100ms hide delay to prevent flicker)

Render toolbar container:
- Background: `bg-surface` (#252525)
- Border: `border border-border-strong` (rgba(255,255,255,0.12))
- Border-radius: `rounded-lg` (8px)
- Padding: `p-1` (4px)
- Shadow: `shadow-[0_4px_12px_rgba(0,0,0,0.40)]`
- Layout: `flex items-center gap-1`
- Entry animation: CSS `animate-[fadeScaleIn_150ms_ease-out]` (define in style.css if not already)

Button component (internal):
- Size: 32x32px (`w-8 h-8`)
- Border-radius: `rounded` (4px)
- Default: transparent bg, icon color `text-text-primary` (#ECECEC)
- Hover: `hover:bg-surface-elevated` (#2F2F2F)
- Active state (when format is on): icon color `text-accent` (#5B9BD5), background `bg-[rgba(91,155,213,0.12)]`
- Icon size: 16px
- Each button has `aria-label` per UI-SPEC: "Toggle bold", "Toggle italic", etc.

**Button order (left to right):**
1. Bold -- `editor.chain().focus().toggleBold().run()`, active: `editor.isActive('bold')`
2. Italic -- `toggleItalic`, active: `isActive('italic')`
3. Underline -- `toggleUnderline`, active: `isActive('underline')`
4. Strikethrough -- `toggleStrike`, active: `isActive('strike')`
5. Divider: 1px solid rgba(255,255,255,0.08), 16px height, 4px horizontal margin
6. Code -- `toggleCode`, active: `isActive('code')`
7. Link -- `editor.chain().focus().toggleLink({ href: '' }).run()` (prompt for URL via window.prompt for now)
8. Divider
9. Heading dropdown button -- shows ChevronDown icon next to current heading indicator

**Heading dropdown:**
- Implement as a local state toggle (`headingOpen` boolean)
- When open: small absolute-positioned dropdown below the heading button
- Three options: H1, H2, H3 -- each calls `editor.chain().focus().toggleHeading({ level: N }).run()`
- Show checkmark or highlight on currently active heading level
- Close on selection or click outside
- Background same as toolbar (#252525), border border-strong, rounded-lg, shadow

Wire into Editor.tsx:
- Import BubbleToolbar
- Render `<BubbleToolbar editor={editor} />` inside the editor container (BubbleMenu handles its own positioning)
  </action>
  <verify>
    <automated>cd /Users/vedpawar2254/Desktop/personalP/newtab/newtab && grep -q "BubbleMenu" components/editor/BubbleToolbar.tsx && grep -q "shouldShow" components/editor/BubbleToolbar.tsx && grep -q "toggleBold" components/editor/BubbleToolbar.tsx && grep -q "toggleItalic" components/editor/BubbleToolbar.tsx && grep -q 'aria-label="Toggle bold"' components/editor/BubbleToolbar.tsx && grep -q "BubbleToolbar" components/editor/Editor.tsx && echo "Bubble toolbar complete"</automated>
  </verify>
  <acceptance_criteria>
    - components/editor/BubbleToolbar.tsx contains `BubbleMenu` import from `@tiptap/react/menus`
    - components/editor/BubbleToolbar.tsx contains `shouldShow` with `from === to` check (empty selection)
    - components/editor/BubbleToolbar.tsx contains `editor.isActive('codeBlock')` exclusion
    - components/editor/BubbleToolbar.tsx contains `toggleBold`, `toggleItalic`, `toggleUnderline`, `toggleStrike`, `toggleCode`
    - components/editor/BubbleToolbar.tsx contains `aria-label="Toggle bold"` and `aria-label="Toggle italic"`
    - components/editor/BubbleToolbar.tsx contains heading dropdown with `toggleHeading`
    - components/editor/BubbleToolbar.tsx contains shadow `0_4px_12px` or `0 4px 12px`
    - components/editor/Editor.tsx contains `BubbleToolbar` import and renders it
  </acceptance_criteria>
  <done>Floating bubble toolbar appears above text selection with Bold, Italic, Underline, Strikethrough, Code, Link, and Heading dropdown buttons. Active states show accent color. Toolbar hides with 100ms delay. Exclusion zones for code blocks and images.</done>
</task>

<task type="auto">
  <name>Task 2: Build slash command extension and menu with categories, filtering, and keyboard nav</name>
  <files>lib/editor/extensions/slash-command.ts, lib/editor/slash-items.ts, components/editor/SlashCommandMenu.tsx, components/editor/SlashCommandList.tsx, lib/editor/extensions.ts, components/editor/Editor.tsx</files>
  <read_first>
    - .planning/phases/02-core-editor/02-RESEARCH.md (Pattern 3: slash command via @tiptap/suggestion, code example)
    - .planning/phases/02-core-editor/02-UI-SPEC.md (slash menu specs: width 280px, max-height 320px, categories, item styling, keyboard nav, animations)
    - lib/editor/extensions.ts (current extension array to add SlashCommand to)
    - components/editor/Editor.tsx (current editor to ensure slash menu renders)
  </read_first>
  <action>
Create `lib/editor/slash-items.ts`:
- Export `SlashItem` interface: `{ title: string; description?: string; icon: string; category: string; command: (editor: Editor) => void }`
- Export `slashItems: SlashItem[]` array with these items grouped by category:

**Text category:**
- Paragraph: icon "Type", command: `editor.chain().focus().setParagraph().run()`
- Heading 1: icon "Heading1", command: `editor.chain().focus().toggleHeading({ level: 1 }).run()`
- Heading 2: icon "Heading2", command: `editor.chain().focus().toggleHeading({ level: 2 }).run()`
- Heading 3: icon "Heading3", command: `editor.chain().focus().toggleHeading({ level: 3 }).run()`

**Lists category:**
- Bullet List: icon "List", command: `editor.chain().focus().toggleBulletList().run()`
- Numbered List: icon "ListOrdered", command: `editor.chain().focus().toggleOrderedList().run()`
- Todo List: icon "CheckSquare", command: `editor.chain().focus().toggleTaskList().run()`

**Media category:**
- Image: icon "Image", description "Embed an image", command: inserts image placeholder (prompt URL via window.prompt for now, or insert empty image node)
- Code Block: icon "Code", command: `editor.chain().focus().toggleCodeBlock().run()`
- Table: icon "Table", description "Insert a table", command: `editor.chain().focus().insertTable({ rows: 3, cols: 3, withHeaderRow: true }).run()`
- Link Embed: icon "Link", description "Embed a link with preview", command: prompts for URL and inserts bookmark node (placeholder until Plan 04 implements BookmarkCard)

**Advanced category:**
- Blockquote: icon "Quote", command: `editor.chain().focus().toggleBlockquote().run()`
- Horizontal Rule: icon "Minus", command: `editor.chain().focus().setHorizontalRule().run()`

Create `lib/editor/extensions/slash-command.ts`:
- Import `Extension` from `@tiptap/core`
- Import `Suggestion` from `@tiptap/suggestion`
- Import `ReactRenderer` from `@tiptap/react`
- Import `tippy, Instance as TippyInstance` from `tippy.js`
- Import `SlashCommandMenu` from the component
- Create extension using `Extension.create({ name: 'slashCommand' })`
- In `addOptions()`: return suggestion config with `char: '/'`
- `command` callback: `editor.chain().focus().deleteRange(range).run(); props.command({ editor })`
- In `addProseMirrorPlugins()`: return `[Suggestion({ editor: this.editor, ...this.options.suggestion })]`
- The `suggestion.render()` function:
  - `onStart(props)`: create ReactRenderer with SlashCommandMenu, create tippy instance anchored to `props.clientRect`, append to body
  - `onUpdate(props)`: update ReactRenderer props and tippy position via `props.clientRect`
  - `onKeyDown(props)`: forward to the SlashCommandMenu's `onKeyDown` handler (arrow keys, enter, escape)
  - `onExit()`: destroy tippy instance and ReactRenderer

Create `components/editor/SlashCommandMenu.tsx`:
- This is the wrapper component that receives Suggestion props and renders SlashCommandList
- Forward ref with `useImperativeHandle` to expose `onKeyDown` method for keyboard handling

Create `components/editor/SlashCommandList.tsx`:
- Receives `items` (filtered SlashItem[]), `command` callback, `editor`
- Manage `selectedIndex` state (default 0)
- Group items by category
- Render container: width 280px, max-height 320px, overflow-y auto, bg-surface (#252525), border border-strong, rounded-lg, shadow `0 4px 16px rgba(0,0,0,0.50)`, padding 8px
- Entry animation: `animate-[slashEnter_120ms_ease-out]` (fade + slide down 4px)
- **Category headers:** 12px uppercase, letter-spacing 0.05em, color rgba(255,255,255,0.35), padding 8px left 4px top/bottom, NOT selectable by keyboard
- **Menu items:** height 36px, padding 8px horizontal, flex row, gap 12px
  - Icon: 18px, color rgba(255,255,255,0.50) from lucide-react (dynamically import by icon name string)
  - Label: 14px, weight 400, color #ECECEC
  - Description (optional): 12px, color rgba(255,255,255,0.35)
  - Selected/hover: bg `rgba(91,155,213,0.12)`, border-radius 4px
- **Keyboard handling:**
  - ArrowDown: increment selectedIndex (skip category headers), wrap to top
  - ArrowUp: decrement selectedIndex (skip category headers), wrap to bottom
  - Enter: call `command(items[selectedIndex])`
  - Escape: return true to dismiss (Suggestion handles removal)
- **Filtering:** Items are pre-filtered by Suggestion utility based on query (text typed after `/`). SlashCommandList receives only matching items.
- **Empty state:** "No match found -- press Esc to close" centered, 12px, text-secondary
- Scroll selected item into view using `scrollIntoView({ block: 'nearest' })`

Update `lib/editor/extensions.ts`:
- Import `SlashCommand` from `./extensions/slash-command`
- Add `SlashCommand` to the extension array returned by `createExtensions()`

Add new keyframes to `entrypoints/newtab/style.css` if needed:
```css
@keyframes fadeScaleIn {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}
@keyframes slashEnter {
  from { opacity: 0; transform: translateY(4px); }
  to { opacity: 1; transform: translateY(0); }
}
```
  </action>
  <verify>
    <automated>cd /Users/vedpawar2254/Desktop/personalP/newtab/newtab && grep -q "slashItems" lib/editor/slash-items.ts && grep -q "Suggestion" lib/editor/extensions/slash-command.ts && grep -q "SlashCommandMenu\|SlashCommandList" components/editor/SlashCommandMenu.tsx && grep -q "selectedIndex" components/editor/SlashCommandList.tsx && grep -q "SlashCommand" lib/editor/extensions.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/editor/slash-items.ts exports `slashItems` array with items in categories: Text, Lists, Media, Advanced
    - lib/editor/slash-items.ts contains "Paragraph", "Heading 1", "Bullet List", "Code Block", "Table", "Blockquote", "Horizontal Rule"
    - lib/editor/extensions/slash-command.ts contains `Extension.create` with `name: 'slashCommand'` or `name: "slashCommand"`
    - lib/editor/extensions/slash-command.ts contains `Suggestion` import
    - lib/editor/extensions/slash-command.ts contains `ReactRenderer`
    - lib/editor/extensions/slash-command.ts contains `tippy`
    - components/editor/SlashCommandList.tsx contains `selectedIndex`
    - components/editor/SlashCommandList.tsx contains "No match found" or "No results"
    - components/editor/SlashCommandList.tsx contains `scrollIntoView`
    - lib/editor/extensions.ts contains `SlashCommand` in the array
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Typing / opens categorized slash command menu below cursor. Arrow keys navigate items, Enter inserts the block, Escape dismisses. Type to filter in real time. Menu shows categories (Text, Lists, Media, Advanced) with icons and labels.</done>
</task>

</tasks>

<verification>
- Selecting text shows bubble toolbar with Bold, Italic, Underline, Strikethrough, Code, Link, Heading buttons
- Clicking toolbar buttons toggles formatting; active state highlighted in accent color
- Toolbar does not appear in code blocks or on images
- Typing `/` opens slash menu below cursor with categorized items
- Arrow key navigation works, Enter inserts block, Escape dismisses
- Typing after `/` filters items
- `npm run build` succeeds
</verification>

<success_criteria>
1. Bubble toolbar appears on text selection and disappears when deselected
2. All formatting buttons in toolbar work correctly
3. Heading dropdown shows H1/H2/H3 options
4. Slash menu opens on `/` keystroke
5. All slash menu items insert correct block types
6. Keyboard navigation (arrows, enter, escape) works in slash menu
7. Filtering by typing works in slash menu
8. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-editor/02-02-SUMMARY.md`
</output>
